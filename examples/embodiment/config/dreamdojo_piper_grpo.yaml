# Backward-compatible alias. New commands should use the explicit teacher name
# or one of the hardware-specific presets.
defaults:
  - dreamdojo_piper_teacher_grpo
  - _self_

runner:
  logger:
    experiment_name: "dreamdojo_piper_grpo"
